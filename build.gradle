plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

sourceCompatibility = 21

subprojects {
    repositories {
        mavenLocal()
        mavenCentral()
        maven {
            url 'https://repo.spring.io/release'
        }
    }
}